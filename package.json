{
    "name": "bmi-calculator",
    "version": "1.0.0",
    "scripts": {
      "build": "tsc && cp *.html dist/ && cp *.css dist/"
    },
    "devDependencies": {
      "typescript": "^5.3.3"
    }
  }